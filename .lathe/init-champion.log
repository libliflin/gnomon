Done. Here's what was written:

**`.lathe/agents/champion.md`** — the runtime playbook. Five stakeholders with concrete first-encounter journeys, emotional signals per stakeholder, tension pairs with resolution signals, the two-source ranking model (CI floor → lived experience), maturation guidance tied to PLAN.md §15, the output format template verbatim, and ownership of inputs.

**`.lathe/skills/journeys.md`** — ten-step concrete journeys for each stakeholder, including where momentum builds, where it breaks, and a stretch target that walks to the project's current ambition wall.

**`.lathe/skills/architecture.md`** — the detection pipeline (HtmlAnalysis → theater_violations), what's implemented vs. what's in PLAN.md but not yet built, the brotli recompression model, and the key seam for adding new checks.

**`.lathe/skills/testing.md`** — the two-test pattern (detect in `analyze.rs`, violation in `audit.rs`), what's intentionally not tested, and the detail string conventions.

**`.lathe/skills/build.md`** — dev commands, CI config, non-obvious dependency choices (rustls, brotli recompression crate).

**`.lathe/skills/domains.md`** — six domains (HTML parsing, anti-theater, HTTP/brotli, SARIF, Web Vitals, Rust), each with authoritative source and the specific boundary confusions that cause bugs.

**`.lathe/alignment-summary.md`** — for you. Flags four things that could be wrong: `ambition.md` is missing, `brand.md` is missing, justification validation isn't enforced, and the snapshot doesn't check remote CI status.
